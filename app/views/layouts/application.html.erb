<!DOCTYPE html>
<html>
  <head>
    <title>Blog App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to "BlogApp", root_path, class: "text-2xl font-bold text-indigo-600" %>
          </div>
          
          <!-- Search Form -->
          <div class="flex items-center flex-1 max-w-md mx-8">
            <%= form_with url: posts_path, method: :get, class: "w-full" do |f| %>
              <div class="relative">
                <%= f.text_field :query, 
                    placeholder: "Search posts...", 
                    value: params[:query],
                    class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
                <%= f.submit "Search", class: "absolute right-2 top-1/2 transform -translate-y-1/2 px-4 py-1 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 cursor-pointer" %>
              </div>
            <% end %>
          </div>
          
          <!-- Navigation Links -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to "New Post", new_post_path, class: "px-4 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition" %>
              <%= link_to "Profile", profile_path, class: "text-gray-700 hover:text-indigo-600" %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-gray-700 hover:text-indigo-600" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-indigo-600" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "px-4 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    
    <!-- Flash Messages -->
    <% if notice.present? || alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% if notice %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline"><%= notice %></span>
          </div>
        <% end %>
        
        <% if alert %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline"><%= alert %></span>
          </div>
        <% end %>
      </div>
    <% end %>
    
    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-white mt-16 border-t">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <p class="text-center text-gray-500">
          &copy; <%= Time.current.year %> BlogApp. Built with Ruby on Rails.
        </p>
      </div>
    </footer>
  </body>
</html>